/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ae-pic-a1.aliexpress-media.com'], // Adicione o domínio aqui
      },
      experimental: {
        appDir: true,
      },
};

export default nextConfig;
